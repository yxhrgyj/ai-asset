# A2 Portal Implementation Plan

**Goal:** Implement the A2 homepage approved on 2026-09-08, with working search and categories and a consistent public portal theme.

**Execution:** The current primary agent executes and reviews every step serially. No subagents or additional Codex tasks.

**Architecture:** Keep Vue 3, Vue Router, Pinia, the public API and the existing isolated worktree. Share a single asset card between home and catalogue. Scope all theme changes to `.portal-layout`.

**Tech Stack:** Vue 3, TypeScript, Vite 6, Lucide Vue, existing Playwright and Chrome.

## Approved Design

- Reference: `C:/Users/Administrator/.codex/visualizations/2026/09/08/01a07e7f-b4bf-7f73-8429-64f3b76dbdb1/portal-style-references/concepts/concept-a-reading-v2.png`, 2048 x 1536.
- White header and page, full-width deep-green search band, recent assets in two columns, restrained footer.
- Tokens: white `#ffffff`, body text `#1d2129`, secondary text `#4e5969`, line `#e5e6eb`, accessible deep green `#086b50`, brand green `#1baa7f`, pale gray `#f7f8fa`. Asset icons keep the specification's blue, orange, green and violet route colors.
- Typography: existing Microsoft YaHei / PingFang / system sans serif, 40px desktop headline, 26px section heading, 22px asset title, 16px summary, 14px metadata. Fixed font sizes with wrapping at compact widths; zero letter spacing.
- Content: retain approved platform name, navigation, headline, search, four categories, recent releases and footer copy. Names, summaries, tags, totals and dates come from the API.
- Layout: centered content, 90% width capped at 1840px; 76px header, deep-green full-width search band, 2-column cards and a simple footer. Mobile uses one column and a menu while keeping management and login reachable.
- Icons: existing brand image, Lucide search/menu/arrows/document/code/package icons, small muted category tiles. Decorative document artwork extracted from the approved image, independent of interactive text.
- Intentional functional adaptations: count and ordering follow live API data; dates use updatedAt with publishedAt fallback; no hardcoded asset IDs or totals. Mobile header wraps the full brand name when needed. Existing help remains reachable from the about page.

## Task 1: Homepage Search and Categories

Files: `frontend/src/views/PortalHome.vue`, existing `frontend/src/lib/portal-navigation.ts`.

- [x] Verify the implemented homepage search input and category controls with Playwright; current regression evidence is recorded in PROJECT_PROGRESS.md.
- [x] Use normalized route query type for category state; request `portalApi.listAssets({ type, page: 0, size: 4 })` and show the returned total.
- [x] Search routes to `/assets-public` with trimmed q and selected type; view-all retains the selected type.
- [x] Preserve newest-response protection across rapid category changes and unmounts; provide loading, empty and retry states.
- [x] Verify category results, URL history, search submission, view-all, empty, retry and delayed requests.

## Task 2: Shared A2 Presentation

Files: `frontend/src/components/PortalLayout.vue`, `frontend/src/components/PortalNav.vue`, new `frontend/src/components/PortalAssetCard.vue`, `frontend/src/views/AssetsPublic.vue`, `frontend/src/portal.css`, `frontend/src/views/StatisticsPublic.vue`, `frontend/public/brand/portal-documents.png`.

- [x] Implement the approved full-name brand/header/footer, responsive navigation and document artwork.
- [x] Create shared asset card with title, summary, subdued tags, scope/type and update date. Use the existing API summary shape.
- [x] Apply scoped green/white colors, update controls and Markdown contrast, and adapt chart legend colors to white.
- [x] Compare native-size and desktop/mobile screenshots with A2 using view_image; fix spacing, typography, color and icon mismatches.
- [x] Verify detail, catalogue, data, about/help and management/login navigation with no runtime errors or horizontal overflow.

## Task 3: Verification and Handoff

- [x] Run `npm.cmd run test:contract` in frontend.
- [x] Run `npx.cmd vue-tsc --noEmit` in frontend.
- [x] Run `npx.cmd vite build --outDir <external-artifact-directory>/build` to preserve existing generated output.
- [x] Run the new Playwright suite and read-only live API smoke checks; record fixture use separately.
- [x] Inspect diff and `git diff --check` for changed source; update PROJECT_PROGRESS.md with results, risks and the exact continuation point.
- [x] Open the working local portal for the user. Cloud deployment remains deferred. The user explicitly authorized merging and pushing this completed work on 2026-09-08.
