# AI 资产平台 UI 设计规范

本文档定义 AI 资产平台的视觉设计标准，所有前端开发必须严格遵循。

---

## 01 色彩

### 主题色

**地球绿 #1BAA7F**

```css
--color-primary: #1BAA7F;
--color-primary-light: #4DC5AC;
--color-primary-lighter: #8AE1BC;
--color-primary-dark: #0C937F;
--color-primary-darker: #074D3D;
```

### 功能色

**成功色 #1FB33C**
```css
--color-success: #1FB33C;
--color-success-light: #66B64A;
--color-success-bg: #E0F9E6;
```

**警告色 #FFB33C**
```css
--color-warning: #FFB33C;
--color-warning-light: #FFD057;
--color-warning-bg: #FFF4E0;
```

**错误色 #F53F3F**
```css
--color-error: #F53F3F;
--color-error-light: #F76560;
--color-error-bg: #FFECEC;
```

### 中性色

```css
--color-text-primary: #1D2129;
--color-text-secondary: #4E5969;
--color-text-tertiary: #86909C;
--color-text-disabled: #C9CDD4;

--color-bg-1: #FFFFFF;
--color-bg-2: #F7F8FA;
--color-bg-3: #F2F3F5;
--color-bg-4: #E5E6EB;

--color-border: #E5E6EB;
--color-border-light: #F2F3F5;
```

### 地球线路颜色（状态标签）

按照规范图片的地球线路颜色使用：

- **1号线（橙色）**: `#FFB020`
- **2号线（红色）**: `#F53F3F`
- **3号线（蓝色）**: `#165DFF`
- **4号线（绿色）**: `#00B42A`
- **5号线（紫色）**: `#722ED1`
- **6号线（青色）**: `#00B8D9`
- **7号线（灰色）**: `#86909C`
- **8号线（粉色）**: `#F54A8D`
- **9号线（青绿）**: `#14C9C9`
- **10号线（黄色）**: `#F7BA1E`
- **11号线（深蓝）**: `#0E42D2`
- **12号线（深绿）**: `#0A754A`

---

## 02 布局

### 固定使用规则

- **UI 界面布局基于** `动态` 原则，并且充分考虑中后台业务场景的色彩应用需求和无障碍设计标准
- **屏幕总宽度**: 1440px（设计稿标准）
- **最小安全宽度**: 1280px

### T 型布局（推荐，导航在左侧）

```
┌─────────────────────────────────────┐
│  顶部栏 (12px 高度)                  │ ← 品牌 logo + 全局导航 + 用户信息
├────────┬────────────────────────────┤
│        │                            │
│ 侧边栏 │    内容区域 (AUTO)          │
│ 180px  │                            │
│        │                            │
│ 固定宽 │    1440px 总宽减去侧边栏    │
│        │                            │
└────────┴────────────────────────────┘
```

- **侧边栏宽度（展开）**: 180px
- **侧边栏宽度（收起）**: 64px
- **内容区域宽度**: `calc(100% - 侧边栏宽度)`
- **内容区域左右内边距**: 24px
- **内容区域上下内边距**: 20px

### 顶部导航栏布局方案（备选）

```
┌─────────────────────────────────────┐
│  Logo | 一级导航 | 二级导航 | User   │ ← 180px 左 + 14px 间距 + 10px 右
├─────────────────────────────────────┤
│                                     │
│         内容区域 (1440px x AUTO)     │
│                                     │
└─────────────────────────────────────┘
```

### 侧边导航栏组件

**展开状态**：
- 宽度: 180px
- 背景色: `--color-primary` (#1BAA7F)
- 文字颜色: `#FFFFFF`
- 选中项背景: `rgba(255, 255, 255, 0.15)`
- 菜单项高度: 48px
- 菜单项左右内边距: 16px
- icon 尺寸: 16px
- icon 与文字间距: 12px

**收起状态**：
- 宽度: 64px
- 仅显示 icon，不显示文字
- tooltip 提示菜单名称

**二级菜单**：
- 缩进: 24px
- 高度: 40px
- 背景色悬停: `rgba(255, 255, 255, 0.08)`

---

## 03 导航

### 顶部导航栏

- 高度: 64px (含 12px 顶部装饰条)
- 背景: 12px 主题绿 + 52px 白色
- Logo 区域: 左侧 180px（与侧边栏对齐）
- 标题文字: 16px Semibold
- 用户信息: 右侧 24px 边距，头像 32px 圆形

### 侧边导航栏层级

**解决一级 - 无折叠**：
```
□ 菜单A
□ 菜单B
□ 菜单C (选中态，绿色背景深一度)
```

**解决一级 - 二级展开**：
```
▼ 菜单A (展开)
  ▸ 二级菜单A1
  ▸ 二级菜单A2 (选中态)
  ▸ 二级菜单A3
▸ 菜单B
▸ 菜单C
```

### Tab 导航

**单选样式**：
- 高度: 48px
- 选中态下边框: 2px 主题绿
- 字重: 选中 Semibold / 未选中 Regular
- 间距: 32px

**筛选组**：
- 高度: 40px
- 选中态: 主题绿背景 + 白色文字
- 未选中: 白色背景 + 灰色文字
- 圆角: 8px
- 内边距: 12px 16px

---

## 04 字体

### 文字字体

**Mac**: PingFang SC  
**Windows**: 微软雅黑  
**移动端**: 调用系统默认  
**数字**: Barlow

```css
font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif;
```

### 字体层级

| 用途       | 字号  | 字重     | 行高  |
|----------|------|--------|-----|
| 特殊标题     | 40px | Semibold | 56px |
| 强调标题     | 32px | Semibold | 44px |
| 一级标题     | 20px | Semibold | 28px |
| 小标题      | 16px | Semibold | 24px |
| 正文内容     | 14px | Semibold | 22px |
| 辅助文字     | 12px | Semibold | 20px |

**普通字重版本（Medium/Regular）同上**

### 数字字体 (Barlow)

| 用途        | 字号  | 字重 |
|-----------|------|-----|
| 一级数值 123  | 30px | Bold |
| 二级数值 123  | 24px | Bold |
| 三级数值 123  | 20px | Bold |

---

## 05 圆角与间距

### 圆角使用规范

圆角遵循 4 的倍数：

- **4px**: 小图标 icon 圆角
- **8px**: 按钮圆角、主要区域卡片和模块
- **12px**: 内容层中，主要用来区分不同数据
- **16px**: 内容层，数据主体最多这一层的圆角大小
- **20px**: 背景层，用途区分内容和模块，属于最大分割层

**使用原则**：
- 20px: 背景层，用途区分内容和模块
- 16px: 内容层，数据主体最多这一层
- 12px: 内容层中，主要用来区分不同数据
- 8px: 按钮圆角，主要出现在可点击或确定和取消单集中
- 4px: 部分数据使用的 icon 圆角

### 间距使用变量

**减掉了页面中常用的所有间距，在元素间距小于问题，元素间距等同规则可使用**

| Name | Value |
|------|-------|
| sp-0 | 0     |
| sp-2 | 2px   |
| sp-4 | 4px   |
| sp-6 | 6px   |
| sp-8 | 8px   |
| sp-10| 10px  |
| sp-12| 12px  |
| sp-14| 14px  |
| sp-16| 16px  |
| sp-18| 18px  |
| sp-20| 20px  |
| sp-24| 24px  |

---

## 06 组件规范

### 按钮

**主要按钮（Primary）**：
- 背景: `--color-primary`
- 文字: `#FFFFFF`
- 圆角: 8px
- 高度: 40px（大）/ 32px（中）/ 24px（小）
- 内边距: 16px 24px（大）/ 12px 20px（中）/ 8px 16px（小）
- hover: 背景加深 10%
- active: 背景加深 20%

**次要按钮（Secondary）**：
- 背景: 透明
- 边框: 1px `--color-border`
- 文字: `--color-text-primary`
- hover: 背景 `--color-bg-2`

**危险按钮（Danger）**：
- 背景: `--color-error`
- 文字: `#FFFFFF`

### 输入框

- 高度: 40px
- 圆角: 8px
- 边框: 1px `--color-border`
- 内边距: 12px 16px
- focus: 边框 `--color-primary`

### 卡片

- 背景: `--color-bg-1`
- 圆角: 12px
- 阴影: `0 2px 8px rgba(0, 0, 0, 0.08)`
- 内边距: 20px

### 表格

- 表头背景: `--color-bg-2`
- 表头高度: 48px
- 行高度: 56px
- 边框: 1px `--color-border`
- hover: 背景 `--color-bg-2`

### 标签（Tag）

使用地球线路颜色区分状态：
- 高度: 24px
- 圆角: 4px
- 内边距: 4px 8px
- 字号: 12px

---

## 07 图标

### Icon 规范

- 使用统一的 icon 库（推荐 Lucide Icons / Heroicons）
- 尺寸: 16px / 20px / 24px
- 颜色继承父元素文字颜色

---

## 08 响应式

### 断点

- **xl**: ≥ 1440px（标准）
- **lg**: 1280px - 1439px
- **md**: 1024px - 1279px
- **sm**: < 1024px（侧边栏自动收起）

---

## 09 暗色模式

**暂不实现**，预留 CSS 变量支持未来扩展。

---

## 10 无障碍

- 所有交互元素必须支持键盘操作
- 对比度符合 WCAG AA 标准（至少 4.5:1）
- 表单必须有明确的 label
- 图标按钮必须有 aria-label

---

## 实施检查清单

开发任何新页面/组件前，必须确认：

- [ ] 使用了正确的主题绿色 #1BAA7F
- [ ] 布局符合 T 型结构（侧边栏 180px）
- [ ] 字体使用系统默认字体栈
- [ ] 圆角使用 4/8/12/16/20px
- [ ] 间距使用 4 的倍数
- [ ] 按钮/输入框/卡片符合规范尺寸
- [ ] 状态标签使用地球线路颜色
- [ ] hover/active 状态已定义
- [ ] 无障碍属性已添加

---

**最后更新**: 2026-09-03  
**维护者**: AI 资产平台开发团队
