# 项目进度：管理工作台与公开门户

最后更新：2026-09-08（Asia/Shanghai）

## 取消审核、下载排行与安全上传（2026-09-08，验证完成）

- 接续任务 `01a07f72-2819-7701-8eb7-e138966b99af` 的最后一条用户需求：取消审核功能，门户首页增加下载排行，新建资产支持 ZIP 和图片，并完善上传安全。此前该条需求因请求失败未执行。
- 保留已实施的 A2 布局和统一绿色主题；当前主智能体直接完成实现、检查与收尾，不启用其他智能体或任务。
- 实施顺序：梳理并移除审核入口及调用，衔接作者直接发布；接入仅包含公开已发布资产的真实下载排行；统一新建/编辑上传类型并补齐服务端内容、压缩包与路径校验。
- 验证计划：发布权限与上传安全的后端回归、前端契约与类型检查、独立目录生产构建，以及首页/发布/上传在桌面和手机上的浏览器验证。
- 本轮基线：分支 `codex/portal-website`，HEAD `864b83b`；A2 源码与历史生成产物已有未提交改动。保留现有文件，构建与截图使用独立输出目录。
- 已完成：移除审核路由页面、菜单、工作台指标和后端接口；作者直接发布，纯附件可发布；新版本继承附件。V8 将待审及最新驳回版本恢复为草稿，审核角色转为作者，保留历史审核表。
- 上传已支持 ZIP、PNG/JPG/GIF/WebP、PDF 及现有文本/源码类型。服务端检查内容、图片尺寸和解码、路径、ZIP 目录/文件头/CRC/加密/链接/嵌套包/解压上限；下载强制附件和 nosniff，草稿访问限负责人或管理员。
- 新建上传失败后复用已创建草稿，成功文件不重复上传；编辑器上传保留未保存正文。修复手机表单、资产卡片和版本选择器溢出，上传期间禁用队列修改，草稿行锁串行化发布与附件修改。
- 2026-09-08 接续任务 `01a08008-72de-7600-b0d4-7ff1e86d6e95`：确认上轮停在后端编译通过，详情仍引用已删除的审核 API；本轮补齐直接发布、新建/详情上传、失败重试和附件访问权限，并执行完整验证。
- 验证：后端 75/75（含 42 项上传安全用例及 8 项直接发布/权限/迁移/排行集成用例），前端契约 25/25，vue-tsc 和独立目录 Vite 构建通过；原门户 A2 Playwright 32/32、新流程 Playwright 10/10 通过。覆盖门户 320-2048px 和新建/详情 320/390/1440px，无页面异常、错误覆盖层或横向溢出。
- 浏览器写操作使用 API 夹具，后端集成测试使用事务回滚与临时附件目录。真实服务只读验证为 UP、4 项已发布资产、下载排行空态；非空排行、上传失败和发布使用测试数据分别验证。未修改真实账号密码或清理业务数据。
- 本轮证据目录：`C:/Users/Administrator/.codex/visualizations/2026/09/08/01a080f7-182c-7eb2-a45c-1458533194f3/`，包含测试日志、两套浏览器脚本和 JSON 结果、桌面/手机截图及独立生产构建。
- 已知限制：保留现有编辑器大分块提示；文件内容检查不等同于病毒扫描。无 Browser 插件，使用已安装 Playwright 与本机 Chrome。
- 用户本轮明确要求“都完成后 合并代码 推送远端”。当前继续点：提交本轮源码及进度，快进合并 main，推送 main 和门户分支，核对远端哈希。云端部署继续暂缓；历史 dist、tsbuildinfo、SQL 和其他本地文件保留。

## A2 门户实施（2026-09-08，已完成）

- 用户已在上一任务明确批准 A2，并要求按效果图修改实际门户；本任务接续该授权。此前中断发生在源码检查阶段。
- 已采用 `writing-plans`、`frontend-design`、`frontend-app-builder`、`test-driven-development` 与前端浏览器验证流程；全部由当前主智能体执行。
- 实施清单：`docs/superpowers/plans/2026-09-08-portal-a2.md`。范围为首页检索与分类、公共页头页尾、统一资产卡片和门户蓝白主题。
- 已恢复本工作区服务：`http://127.0.0.1:5173/`，后台 `http://127.0.0.1:8080/actuator/health`；启动脚本完成后端打包并检测健康。
- 验证基线：现有 `frontend/dist` 和 `frontend/tsconfig.tsbuildinfo` 有历史未提交产物，本轮构建输出写入工作区外；当前未改后端源码。
- 本轮接续完成 A2 实施与验证：检索、分类、历史导航、空态和重试、共享卡片、统一绿色主题，以及 12 种视口和各公开页面浏览器回归均通过，详见上方本轮验证记录。
- 视觉统一决策：按 UI 规范将门户主交互色从独立蓝色调整为可读深绿 `#086B50`，顶部品牌条保留主题绿 `#1BAA7F`；蓝、橙、紫仅作为资产图标/图表的辅助色。该调整保留 A2 深绿搜索区预览的结构与层级。
- 云端部署继续暂缓；本轮 GitHub 发布按最新用户授权执行，状态见上方记录。

## 门户首页效果图评审（2026-09-08，A2 已交付）

- 用户已选择重点参考 01 Dify Marketplace、02 Notion 模板、05 Glean，要求先看效果图再决定调整方向。
- 最新反馈：用户明确排除 C，A 目前最满意；了解 B 的“阅读感”指文字层级、间距与辅助信息处理后，同意按建议继续调整。
- 已交付 A2 效果图：保留 A 的蓝白配色、搜索区、分类和双列卡片，细化标题与简介排版、减轻图标和技术标签的分量，将卡片底部整理为范围/类型与更新时间。四项资产继续通过本轮公开 API 核实。
- A2 原图：上述 `concepts/` 目录的 `concept-a-reading-v2.png`（2048×1536）。本轮以原版 A 和项目 Logo 为参考，Fast 任务 `fc6f3cba-1fad-4eec-a6b5-0b802fb1ca07` 已成功下载；提示词、任务记录和原图均保留。
- A / A2 对比页：`http://127.0.0.1:58153/concepts/refined-a.html`，支持并排、单版查看、图片放大、原始尺寸及键盘切换。Chrome + Playwright 15 项通过，1600px / 390px 无横向溢出与页面错误，已目视核对 A2 原图及桌面/手机对比页截图。验证记录为 `refined-a-checks.json`；第一轮页面新增了本轮入口。
- 已交付三版 2048×1536 完整桌面首页概念：A 清爽资产中心（Dify 为主）、B 友好知识库（Notion 为主）、C 企业品牌门户（Glean 为主）。均使用 Fast Image Generation，参考现有 Logo、平台名称、导航和本轮公开 API 核实的四项真实已发布资产；无虚构资产量、下载量或额外 AI 能力。
- 本轮只制作独立设计评审材料，前端实现继续等待视觉方向确认。所有工作由当前主智能体执行，遵守全局禁止子智能体规则。
- 输出目录：`C:/Users/Administrator/.codex/visualizations/2026/09/08/01a07e7f-b4bf-7f73-8429-64f3b76dbdb1/portal-style-references/concepts/`。保留提示词、生成任务 ID 和结果，便于继续修改。
- 三版图片均已按原始尺寸逐张检查标题、导航、搜索、资产名称、说明、页尾和整体布局。交互评审页为上述目录的 `index.html`，可通过 `http://127.0.0.1:58153/concepts/index.html` 查看；`overview.png` 为并排总览，`manifest.json` 记录尺寸、来源方向、生成任务 ID 与文件校验信息。
- 评审页使用本机 Chrome + Playwright 完成 20 项展示与交互检查：三图加载、单版查看、放大、原始尺寸、键盘切换、退出、偏好记录，以及 1600px / 390px 下无横向溢出；无页面错误。已查看桌面和手机截图。此验证范围为设计评审页，不代表效果图中的门户交互已经实现。
- Fast 生成任务：A `46442ff9-58d8-421d-9227-f12ad74d39ea`，B `083f8f31-4622-43c9-89b2-6476814a245d`，C `bb986a0b-adbb-43aa-b61f-c25b67469f91`，均成功下载并保留原图。
- 当前继续点：基于 A / A2 对照继续确认细化效果，再补齐所需页面或移动端概念并进入实现。C 已排除，原稿保留为历史评审材料。本轮完成的是效果图细化与评审页，门户前端源代码尚未改版。

## 门户视觉参考调研（2026-09-08）

- 用户认为当前门户样式不理想，要求先分享合适的真实案例，由用户决定调整方向。
- 已访问并截图：Dify Marketplace、Notion 模板、魔搭模型库、Vercel Templates、Glean、Raycast Store 六个主案例；Hugging Face Models 作为补充目录结构参考。同时截取当前首页用于对照。
- 已交付本地交互对比页，包含原网站链接、完整截图、借鉴部位、门户适配建议与取舍；支持方向筛选、截图放大和选择案例。桌面与 390px 检查通过，图片正常加载，筛选、放大、选择交互正常，无页面错误或横向溢出。
- 参考文件：`C:/Users/Administrator/.codex/visualizations/2026/09/08/01a07e7f-b4bf-7f73-8429-64f3b76dbdb1/portal-style-references/index.html`；总览图为同目录 `overview.png`，原始截图及来源记录为 `captures.json` / `cases.json`。参考页服务当前为 `http://127.0.0.1:58153/`，可从该目录运行 `node serve-gallery.cjs` 重新启动并读取 `server.json` 中的新地址。
- 提出三条待用户选择的方向：清爽资产中心（优先建议 Dify 结构 + Notion 阅读感）、品牌门户配专业目录（Glean 首页 + 魔搭/Vercel 目录）、深色专业工具（Raycast 层次 + Dify 卡片）。
- 用户随后选择了 Dify Marketplace、Notion 模板和 Glean，已进入首页效果图评审阶段；具体改版方案尚未确认。

## 合并完成（2026-09-08）

- 已按用户要求将 `codex/portal-website` 快进合并到 `main`，并成功执行 `git push --atomic origin main codex/portal-website`。
- 源码提交为 `78d208cbd474f32631ad06c9b06a5736a695754c`；代码发布后，`git ls-remote --heads origin main codex/portal-website` 确认两个远端分支与本地该提交一致。本进度记录通过后续文档提交同步。
- 修复了前一任务中断后返回的审查项：不再在宽度不超过 980px 时隐藏管理中心入口；手机宽度下缩小导航间距，保留 Logo、菜单、管理入口与用户状态。
- Playwright + Chrome 在 320/390/680/681/768/980/981/1440px、已登录与访客两种状态下完成 16 项浏览器验证：修复前 12 项因入口隐藏而失败，修复后 16/16 通过。覆盖键盘进入管理中心、访客到登录页、导航开合、资产库跳转、控制台与横向溢出；已查看手机、平板截图。
- 功能分支与合并后的 `main` 均通过前端契约测试 23/23、后端测试 25/25；后端失败、错误、跳过均为 0。`vue-tsc --noEmit`、Vite 生产构建、两个 Node 启动脚本语法检查通过。
- 使用 `node scripts/start-local.cjs --no-browser` 重新打包并恢复本地服务；本轮后端健康检查为 `UP`，通过 5173 前端代理读取公开统计仍为 4 项资产。启动时后端 PID 10332、前端 PID 4552，后续操作前应重新核对。
- 浏览器认证状态使用 API 夹具；在线健康和公开统计为真实服务只读检查。未重置账号密码或清理业务数据，后端新增测试使用事务回滚。
- 两个工作区现有的生成产物及主目录 SQL 文件共 252 个，合并后 SHA-256 核验全部一致。构建输出位于独立目录，原有分支、工作区和未提交文件均保留。
- 本轮截图、导航复验脚本、结果、文件校验基线及独立构建：`C:/Users/Administrator/.codex/visualizations/2026/09/08/01a07e7f-b4bf-7f73-8429-64f3b76dbdb1/merge-main-20260908/`。当前无 Browser 插件，复用已安装的 Playwright 1.62.1 和本机 Chrome。
- 门户实现与本轮源码差异空白检查通过。相对原远端 `main` 的完整差异仍会报告两份既有计划/设计文档的行尾空格（包含 Markdown 显式换行）；已确认这些文档与合并前本地主线 `ad21b54` 一致，未改动。Vite 现有 AssetDetail 大分块提示保留。
- 云端部署继续暂缓；本轮完成 GitHub 主线发布和本地验证，未执行云端部署。

## 本轮工作台状态

- 2026-09-07 首页与平台统计合并为 `/admin` 工作台，旧 `/admin/statistics` 和 `/statistics` 保留查询参数与锚点跳转。`Home.vue` 仅保留兼容包装，不再维护一套重复统计。
- 保留“数科院AI资产管理后台”全称及用户提供的 Logo；菜单使用统一 Lucide 图标，展开/折叠宽度 180/64px、条目高 48px、图标 16px，低于 1024px 自动折叠。详情页保留所属菜单高亮；键盘提示、Escape 关闭和低高度侧栏滚动已验证。
- 工作台提供 7 项真实指标、按角色显示的快捷入口、热门资产、活跃用户、最近下载，以及刷新、失败重试和空态。移除原首页硬编码的“项目绑定 0”。
- 修正已发布资产统计：只计未归档且至少存在一个 PUBLISHED 版本的资产；多个发布版本不重复计数，草稿和无版本资产不计入。回归测试事务回滚，不更改用户数据。
- 用户追加的视觉调整已实现：团队默认表情替换为浅绿底 Lucide 团队图标，编辑/删除按钮有明确提示；工作台快捷入口统一为 48px 等高按钮，提供分色图标、站内右箭头及 hover/focus 状态。
- 规范例外：原主题绿 `#1BAA7F` 配白字对比约 2.95:1，不能满足规范自身的 AA 要求。仅侧栏使用 `#086B50`（白字约 6.50:1，选中白色 15% 遮罩约 4.70:1）；顶部 12px 品牌条仍用主题绿。

## 本地运行与门户

- 2026-09-07 本地运行阻塞已解除：Windows JDK 的 Unix-domain socket 连接失败，启动时把 `jdk.net.unixdomain.tmpdir` 指向不存在的目录，使内部 Pipe 回退到 TCP。此为本机兼容措施。
- 后端常驻启动命令：`node scripts/start-local-backend.cjs`（需先构建 JAR）；日志 `target/local-backend.log`，监听 `127.0.0.1:8080`。脚本检查端口，避免重复启动。
- 推荐一键入口 `start-local.cmd`；无浏览器启动验证可用 `node scripts/start-local.cjs --no-browser`。2026-09-07 22:16 已通过该脚本重新打包并启动当前后端（当时 PID 4632），复用健康的前端；后续操作前应重新确认进程归属。
- 附件默认复用相邻原项目的 `data/asset-files`，可通过 `ASSET_STORAGE_ROOT` 指定；数据库继续使用现有 PostgreSQL。
- 当前真实 `/actuator/health` 返回 UP；经 5173 前端代理读取公开统计为 4 项已发布资产。此前关于后端无法启动的描述为历史状态。

- 实现位于隔离工作区 `E:/Objects/ai-asset-platform-portal`，分支 `codex/portal-website`。
- 已实现公开首页、资产目录与详情、数据台、关于和帮助页面；管理界面位于 `/admin/*`，旧地址保持兼容。
- 公开 API 在分页前排除未发布和归档资产；详情只提供已发布版本，编辑入口同时校验作者权限与所有权（或管理员身份）。
- 登录和修改密码保留经过校验的返回地址；目录筛选支持 URL 状态和浏览器后退；Markdown 继续使用 DOMPurify 清理。
- 下载复用已有认证导出端点，导出响应成功后记录所选版本下载；失败显示错误且不计数。
- 后台使用可折叠侧栏；所有工作台快捷入口和项目跳转使用 `/admin/*`。

## 本轮验证证据

- 前端 `npm.cmd run test:contract`：23/23 通过；页面合并、团队图标、快捷入口及 Escape 边界测试先失败后修复通过。
- 后端 `mvn.cmd test`：25/25 通过，失败、错误、跳过均为 0；使用真实 PostgreSQL。新增已发布计数回归曾复现“期望 4，实际 6”，修复后通过。
- 前端 `npm.cmd run build` 与后端 `mvn.cmd package -DskipTests` 成功。首次打包被运行中的 JAR 文件锁拦截，确认并停止本工作区旧后端后重新打包成功。
- Playwright + 本机 Chrome 工作台/团队回归 14/14、门户回归 13/13 通过。工作台覆盖 320/390/768/1023/1024/1280/1440/1920px 宽度与 320px 低高度，刷新、错误重试、空数据、角色权限、旧路由、详情页高亮及键盘操作。
- 独立代码审查发现并已修复两处导航边界：高亮改用匹配路由记录，兼容尾斜杠和大小写；Escape 仅关闭已展开的小屏导航，不从编辑对话框抢走输入焦点。浏览器分别先复现失败，再确认修复通过。
- 页面身份、非空内容、框架错误覆盖层、控制台、交互及截图检查通过；故障用例刻意触发的 503 为预期响应。当前无 Browser 插件，使用现有 Playwright 与本机 Chrome，未新增浏览器依赖。
- Logo 在门户、登录、管理、改密页的桌面/移动尺寸均成功加载，favicon 返回 200。
- 上述浏览器权限及错误场景使用 API 夹具；真实在线冒烟另外验证了后端 UP、门户 4 项已发布资产和详情，无夹具。未使用管理员真实密码执行在线管理写操作，也未修改密码或种子数据。
- 截图、脚本与结果：`C:/Users/Administrator/.codex/visualizations/2026/09/05/01a07128-75c2-75b2-bd2b-349583f96ac1/portal-20260906/`。
- 当前门户：`http://127.0.0.1:5173/`；工作台：`http://127.0.0.1:5173/admin`；团队：`http://127.0.0.1:5173/admin/teams`。
- 源码差异检查通过；全工作树差异仍有历史已跟踪生成文件 `frontend/dist/index.html` 的换行空白提示。`frontend/dist` 和 `frontend/tsconfig.tsbuildinfo` 保留本地且不纳入本轮源码提交。
- 门户统计页分块约 452 kB；现有后台 AssetDetail 分块约 903 kB，仍有 Vite 大分块提示。未在本次 UI 调整中扩大到编辑器打包优化。

## Cloudflare 部署评估

- 已按 `deploying-cloudflare-pages-d1` 技能评估，尚未执行云端部署。
- Vue 前端可部署到 Pages：构建目录 `frontend`，命令 `npm ci && npm run build`，产物目录 `dist`。
- Spring Boot 与 PostgreSQL 不能原样运行于 Pages Functions 和 D1。保留现有后端时，需要外部 Java 服务及同源 `/api` 代理，并验证会话 Cookie 和文件导出。
- 全量迁入 Pages + D1 需要另行迁移 Java API、PostgreSQL 查询及模式、认证会话和附件存储，不属于一次静态前端部署。

## 精确续作点

1. 2026-09-08 门户、工作台、Logo、启动脚本、测试及移动端管理入口修复均已合并并发布到 `origin/main`；`codex/portal-website` 同步保留。主线工作区为 `E:/Objects/ai-asset-platform`，当前运行的门户来自 `E:/Objects/ai-asset-platform-portal`；这次门户分支合并工作已完成。
2. 用户可使用现有账号在本地验收工作台和团队图标。后续管理写流程验收应使用明确授权的测试数据，不重置 admin 密码、不清理业务数据。
3. 云端部署按用户要求暂缓。未来保留 Java 后端需先确定托管位置和 `/api` 代理；D1 方案需要独立迁移计划。

## 推送前复验（2026-09-07）

- 重新执行前端契约测试 23/23、后端测试 25/25，均通过；前端生产构建成功，已有 AssetDetail 大分块提示保留。
- 两个 Node 启动脚本语法检查和源码差异空白检查通过。
- 发布范围不包含 `frontend/dist`、`frontend/tsconfig.tsbuildinfo`、日志、数据库和本地附件；这些文件不删除、不回退。
- `git push -u origin codex/portal-website` 成功，分支已建立上游；`git ls-remote --heads origin codex/portal-website main` 确认功能提交一致，远端 `main` 保持 `51796d3d7c05507dd3b9dc7ea292bee65223eb17`。未创建 PR、未部署云端。

---

以下保留上一轮项目管理修复记录，日期和状态属于历史证据。

# 历史记录：项目管理模块复查与修复

最后更新：2026-09-05（Asia/Shanghai）

## 当前状态

- 项目管理桌面主流程已修复并通过自动化、HTTP 与浏览器验收。
- 修复已提交并推送到 `origin/main`；未重置或清理原有工作区文件，生成产物和 SQL 脚本仍保持本地未提交状态。
- 工作区在本轮开始前已有大量业务改动和未跟踪文件，后续集成必须继续按文件审查，不能整体回退。
- 390px 移动视口仍有横向溢出，暂未擅自重做全局导航；见“已知风险”。

## 已完成修复

### 前端

- API 客户端统一返回类型化业务数据，并确保请求路径只带一个 `/api` 前缀。
- 项目与团队 API、项目列表、项目详情和规则管理统一使用同一响应契约。
- 项目列表和详情恢复共用的 `MainLayout`；移除不存在的 `/project-rules` 导航。
- 合并规则字段改为后端实际的 `rules`、`orgCount` 等字段；移除时使用 `assetVersionId`。
- 添加规则只提供规则资产的已发布版本；规则正文使用转义文本渲染，不使用 `v-html`。
- 修正用户角色、团队名称、重置密码及 email 请求契约。
- 修复严格 TypeScript 构建相关的未使用符号、DOM ref、模块声明和 CSS 声明问题。
- 页面级重试现在会把项目列表和团队列表作为一个整体重新加载，避免掩盖团队请求失败。
- 增加内联 favicon，消除浏览器默认请求 `/favicon.ico` 产生的 404 控制台错误。

### 后端与安全边界

- 项目列表合并当前用户拥有的项目和其 `users.team_id` 对应团队项目，并排除归档项、按 ID 去重。
- 创建/更新请求增加必填边界；归档项目拒绝更新、重复归档、添加规则和移除规则（409）。
- 添加规则时：缺失版本返回 404，草稿版本返回 400，同一规则资产的其他版本重复绑定返回 409。
- `/api/test-data/**` 要求管理员权限；`TestDataController` 仅在显式 `test-data` profile 下启用。
- 数据库诊断程序改为从环境变量读取连接参数，不再内置凭据。

## 设计与计划记录

- `abc5e43`：`docs/superpowers/specs/2026-09-04-project-management-repair-design.md`
- `3d24bd6`：`docs/superpowers/plans/2026-09-04-project-management-repair.md`
- 当前分支：`main`。本次实现提交已完成远端哈希核验：`origin/main` 与本地提交一致。

## 验证证据

### TDD 回归证据

- 原项目控制器回归检查在修复前为 8 项中 6 失败、2 错误；归档后的归档/移除请求错误返回 204。
- 测试数据安全检查在修复前证明匿名用户和普通用户均可收到 200。
- 2026-09-05 新增“项目与团队一起重试”检查，先得到 9 项中 1 个预期失败，最小修复后转绿。
- 2026-09-05 新增 favicon 检查，先得到 10 项中 1 个预期失败，补充图标声明后转绿。

### 2026-09-05 最终自动化

- `cd frontend && npm.cmd run test:contract`：10/10 通过，退出码 0。
- `cd frontend && npm.cmd run build`：`vue-tsc -b` 与 Vite 生产构建成功，退出码 0；保留一个大分块警告。
- `mvn.cmd test`：16/16 通过（ProjectController 10、TestDataSecurity 5、HashPassword 1），失败/错误/跳过均为 0，退出码 0。

### 浏览器验收

- 环境：`http://localhost:5173`，Playwright 1.62.1 + 本机 Chrome；当前会话无 Browser 插件。
- 桌面视口 1440×960：登录 → 项目列表 → 查看详情 → 项目级规则管理 → 添加规则对话框通过。
- 列表显示 1 个现有项目且共用顶栏/侧栏；详情显示 6 张合并规则卡片；管理页显示 1 条绑定和 4 个可选已发布版本。
- 最终复验中 console error/warn、page error、失败请求和 HTTP 4xx/5xx 均为 0。
- 截图保存在 `C:/Users/Administrator/.codex/visualizations/2026/09/04/01a06cb8-58b4-7ca1-ace1-01f84781bf19/`，文件名以 `project-*-final.png` 结尾。

### 只读 HTTP 冒烟

- 现有 8080 进程：`/actuator/health` 为 200/UP；登录、`/api/auth/me`、`/api/projects`、`/api/teams` 均为 200。
- 项目和团队响应均为数组；当前各返回 1 条。冒烟未创建、更新、归档或清理任何数据。

## 运行环境限制

- 现有 8080 是修复前已启动的旧 Java 进程，只能用于前端/API 形状的只读冒烟，不能证明当前后端实现已被该进程加载。
- 当前代码曾尝试在 18080 启动全新后端：编译、数据库连接和 7 个 Flyway 迁移均完成，但 JDK 21 在创建 Tomcat selector 时因 `Unable to establish loopback connection` / `UnixDomainSockets.connect0: Invalid argument` 失败。打包 JAR和更换 Unix-domain 临时目录仍相同，判定为本机运行时限制，不再无效重试。
- 当前后端行为以新鲜的编译、JUnit/MockMvc 16/16 结果为依据。
- 不要对现有 8080 调用 `/api/test-data/clean`；旧进程可能仍加载修复前的匿名破坏性端点。

## 已知风险

- 390×844 视口实测 `documentElement.scrollWidth = 548`，大于 390px；固定 180px 侧栏使内容区拥挤并产生横向溢出。需要单独确认移动导航方案后再改。
- 生产构建中 `AssetDetail` JavaScript 分块约 905.79 kB，超过 Vite 500 kB 提示线；不阻断本次项目管理修复，但后续可做编辑器/语法包按需加载。
- 工作区仍包含本轮开始前的生成产物变化、SQL 文件和其他业务模块改动；它们没有被删除或回退，也不能仅凭当前差异自动判定归属。

## 精确续作点

1. 若用户批准移动端改造，先确认“折叠侧栏/抽屉导航”方案，再针对 390px、768px 和桌面视口实施与截图回归。
2. 若移动端暂不处理，下一步是按文件审查仍保留在本地的生成产物和 SQL 脚本；它们未进入本次提交。
3. 集成前重新运行 `npm.cmd run test:contract`、`npm.cmd run build`、`mvn.cmd test` 和 `git diff --check`。
